# Lox interpreter

Adapted from craftinginterpreters.com

## Building

Using rust nightly (for box patterns) : 

```rust
cargo build --release
```

## Running

In REPL mode:
```rust
cargo run --release
```

From a file:
```rust
cargo run --release <file>
```
